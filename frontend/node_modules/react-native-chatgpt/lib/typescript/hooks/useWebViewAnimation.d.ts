import { Animated } from 'react-native';
interface Arguments {
    onAnimationStart?: (mode: 'hide' | 'show') => void;
    onAnimationEnd?: (mode: 'hide' | 'show') => void;
}
export default function useWebViewAnimation({ onAnimationStart, onAnimationEnd, }: Arguments): {
    animatedStyles: {
        webview: {
            transform: {
                translateY: Animated.AnimatedInterpolation<string | number>;
            }[];
        };
        backdrop: {
            opacity: Animated.AnimatedInterpolation<string | number>;
        };
    };
    animateWebView: (mode: 'hide' | 'show') => void;
};
export {};
//# sourceMappingURL=useWebViewAnimation.d.ts.map